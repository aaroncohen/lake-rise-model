__pycache__/
*.pyc
.venv/
*.egg-info/
.pytest_cache/
.DS_Store
.env
# Mutable runtime state written by the alerting layer — not version-controlled.
artifacts/alert_state.json

# Calibration pipeline runtime state (generated; persisted on the deploy volume)
artifacts/calibration_state.json
artifacts/versions/*.json
data/continuous/*.json
